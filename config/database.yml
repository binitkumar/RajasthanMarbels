development:
    adapter: mysql2
    database: raj_marbles_development
    encoding: utf8
    username: root 
    host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
    adapter: mysql2
    database: raj_marbles_test
    encoding: utf8
    username: root
    host: localhost

production:
    adapter: mysql2
    database: raj_marbles_production
    encoding: utf8
    username: root
    host: localhost
